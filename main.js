<script>


default function, panel hamburger menu (panelBtn,menulink){
    const d= document;
    
 d.addEventListener("click,e=>"(
     if(e.target.matches(panelBtn)) || e.target.matches((panelBtn) * )){
        d.querySelector (panelBtn.classlisten.toggle("is-active");
     }
     
      if (e.target.matches(panelBtn))
      
  d.querySelector(panel.classlist.remove(
    
</script> 
